services:
  qwen3-tts:
    image: ghcr.io/adriabama06/qwen3-tts-docker:main
    build:
      dockerfile: Dockerfile
      context: .
    network_mode: bridge
    container_name: qwen3-tts
    tty: true
    stdin_open: true
    healthcheck:
      test: curl --fail http://localhost:8000 || exit 1
      interval: 60s
      retries: 5
      start_period: 20s
      timeout: 10s
    ports:
      - 8000:8000
    volumes:
      - ./entrypoint.sh:/Qwen3-TTS/entrypoint.sh
      - ./models:/Qwen3-TTS/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all # To use specifics GPUs --> device_ids: ['0']
              capabilities: [gpu]

